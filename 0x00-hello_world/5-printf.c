#include <stdio.h>

/**
* main - Entry point
* Description: This function prints the string ""with proper grammar, but the
* outcome is a piece of art", followed by a new line character, to the console.
* Return: Always returns 0 to indicate success
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
